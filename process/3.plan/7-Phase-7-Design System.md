## Design System (RQ-DSN, ADR-JUC-014 / ADR-JUC-015)

Unified graphical design system: a single source of truth for colour,
typography, geometry and motion, so the app's visual maintenance goes through
tokens instead of scattered literals in the JUCE code.

| Task | Description | Tier | RQ / ADR |
|------|-------------|:-:|---|
| TASK-JUC-090 | `DesignTokens.hpp` three-tier token module (global→semantic→component). **Status: Done — superseded as *generated* by TASK-JUC-095.** | M | RQ-DSN-001..024, ADR-JUC-014 |
| TASK-JUC-091 | Migrate `XplorerLookAndFeel` to tokens (value-preserving). **Status: Done — build 0 err, 81/81 tests.** | M | RQ-DSN-061 |
| TASK-JUC-092 | Migrate `BackgroundRenderer` palette + font scale to tokens. **Status: Done.** | M | RQ-DSN-061, ADR-JUC-013 |
| TASK-JUC-093 | Migrate `ModMatrixPanel`, `SettingsDialog`, `ProgressWindow`, `MainComponent` LED panel to tokens. **Status: Done.** | M | RQ-DSN-061 |
| TASK-JUC-094 | Verify: build + 81/81 functional tests + Xvfb smoke + grep (no raw colour literal outside the module) + value-identity by construction. **Status: Done.** | S | RQ-DSN-071, RQ-DSN-090 |
| TASK-JUC-095 | `juce/tools/design-tokens.yaml` single source + `generate_design_tokens.py` emitting `DesignTokens.hpp` (generated, `--check` idempotent). Value-identical to the hand-written header (27 colours, 0 ARGB diff). **Status: Done — regenerated, build 0 err, 81/81 tests.** | L | RQ-DSN-060, RQ-DSN-063, ADR-JUC-015 |
| TASK-JUC-096 | Move + refactor the mockup generator to `juce/tools/generate_background_mockup.py`, reading palette/fonts/line-width from the YAML via the shared `resolve()`; geometry unchanged. Regenerated SVG byte-identical. **Status: Done.** | M | RQ-DSN-063, ADR-JUC-013, ADR-JUC-015 |
| TASK-JUC-097 | Move `background-mockup.svg` out of `process/` to `juce/tools/`; update path/mechanism references in ADR-JUC-013, ADR-JUC-014, RQ-DSN-060/063; folder-layering rule (process=spec, tools=scripts, src=impl). **Status: Done.** | S | ADR-JUC-015 |
| TASK-JUC-102 | Rotary knob unlit-ring colour fix (added 2026-07, owner visual review): the `controlTrack` token was an opaque `#282E36` slate, reading as a distinctly dark ring — the reference `KnobControl.cs` (`DEFAULT_KNOB_LED_BACKGROUND_COLOR`, Standard style) draws it as `ARGB(5,255,255,255)`, a ~2% white wash over the panel, effectively near-invisible. Token corrected: `semantic.controlTrack` repointed from `global.slate700` (now unused, removed) to `global.white`, new `global.knobTrackAlpha` aliased in `component.knobTrackAlpha`; `XplorerLookAndFeel::drawRotarySlider` now does `controlTrack.withAlpha(knobTrackAlpha)`. **Status: Done, superseded by TASK-JUC-103 (initial 5/255 value too subtle in practice).** | S | RQ-DSN-061, ADR-JUC-014 |
| TASK-JUC-103 | Rotary knob unlit-ring visibility bump (added 2026-07, owner follow-up on TASK-JUC-102): the literal reference alpha (5/255 ≈ 2%) read as fully invisible once combined with our transparent knob body (ADR-JUC-009 dropped the reference's filled gradient body) — at minimum value a knob had no visible shape at all. `global.knobTrackAlpha` raised from `0.0196` to `0.2`, a deliberate deviation from the reference literal since our simplified (bodyless) knob needs the ring alone to carry the control's visibility. **Status: Done — regenerated `DesignTokens.hpp` (`--check` clean), rebuilt (XplorerApp, exit 0), test suite green (81/81), verified under Xvfb: ring clearly visible at every value including minimum.** | S | RQ-DSN-061, ADR-JUC-014, ADR-JUC-009 |
| TASK-JUC-098 | Spacing / layout token scale (added 2026-07, session `feature/DSN`, owner-approved base unit): audited every hand-authored layout call in `SettingsDialog.cpp`/`ProgressWindow.cpp` (only two files with ad hoc positioning; `PageFamilyBlock`/`MainComponent` use control-table coordinates, out of scope) — every recurring gap/margin literal (2,4,6,8,12,24) already divided evenly into a 4px base unit, so this turned out to be value-preserving after all (no pixel changed), same as the colour/typography migration. New global scale `space2/4/6/8/12/24` + two content-driven sizes `dialogRowHeight`(28)/`dialogLabelWidth`(150); semantic roles `layoutHairline/FieldGap/SectionGap/ButtonGap/Margin`, `dialogRowHeight/dialogLabelWidth/progressBarHeight`; both dialog files retrofitted to consume the tokens instead of local literals (existing local `constexpr` names kept, now initialised from tokens — same pattern as `BackgroundRenderer.cpp`'s `LINE_WIDTH`). **Status: Done — regenerated `DesignTokens.hpp` (`--check` clean), rebuilt (XplorerApp, exit 0), test suite green (81/81), Settings dialog verified under Xvfb (opened via Tools→Settings, layout pixel-identical).** | M | RQ-DSN-020, ADR-JUC-014 |

| TASK-JUC-104 | About box (added 2026-07, owner request, `feature/DSN` branch — general GUI port, not design-tokens work): rewrote `showAboutDialog` (`Dialogs.cpp`), replacing the generic `AlertWindow` message box with a new `AboutContent` component strongly inspired by the reference `AboutForm` (`Xplorer/View/AboutForm.Designer.cs`) — found the exact source image via `Resources.resx` (`About` → `..\xdata\About.jpg`, already preserved in this repo's `xdata/` from the .NET/JUCE split), embedded as `juce/app/assets/about.jpg` (`XplorerAssets` binary data). Fixed 474×261 white dialog: image docked left (140px, stretched to fill height), title/version/copyright/GitHub-link block at x=146, separator line, GPL v3 notice + licence link near the bottom (`juce::HyperlinkButton`, launches the system browser); labels set non-interactive so background clicks fall through to the dialog's own `mouseUp`, closing it via `exitModalState` like the reference's whole-form Click handler. **Status: Done — rebuilt (XplorerApp, exit 0), test suite green (81/81), verified under Xvfb (Help→About): layout matches the reference closely, both links present, click-to-dismiss confirmed working (first click after opening only activates the window under headless Xvfb without a window manager — an artefact of the test rig, not the app; a second click closes it as expected).** Owner review follow-ups (post-merge, on `main`): (1) links read as centred instead of left-aligned — `HyperlinkButton` defaults to `Justification::centred`, fixed with `setJustificationType(centredLeft)` (commit `94ca752`); (2) links still read ~4px left of the labels above/below — root cause was `juce::Label`'s default `BorderSize<int>{1,5,1,5}` (text at local x=5) vs `HyperlinkButton::paintButton`'s `reduced(1,0)` (text at local x=1), fixed by shifting both links right by a new `LINK_LABEL_INSET_COMPENSATION = tokens::global::space4` (commit `9bfce31`); (3) the copyright→GitHub-link gap (20px) didn't match the notice→licence-link gap (19px, further thrown off by `_notice` being sized `ROW_HEIGHT*2` for text that fits one line) — "le design système DOIT s'appliquer partout": normalised `_notice` to `ROW_HEIGHT`, and both link rows now derive their Y from `<label row> + ROW_HEIGHT + LABEL_TO_LINK_GAP` with `LABEL_TO_LINK_GAP = tokens::semantic::layoutHairline`, one shared token-derived expression instead of two hand-picked literals (commit `30865ec`). Rebuilt (XplorerApp, exit 0), verified under Xvfb (Help→About, 300%-zoomed crop): both label→link gaps now visually identical. | M | RQ-GUI-025, ADR-JUC-014 |

### FM destination selector + active signal-path overlay (RQ-GUI-038/039, ADR-JUC-016)

Added 2026-07-22, owner request. ADR-JUC-016 **Accepted** (single ADR, overlay-first).
`session.unit_tests = false` → no unit tests; functional verification via the Xvfb/screenshot
pipeline (owner rule "toujours des tests fonctionnels"). Present for DoR approval before starting.

| Task | Description | Tier | RQ / ADR |
|------|-------------|:-:|---|
| TASK-JUC-105 | **FM destination two-way radio selector.** Replace the `ControlKind::RadioButtonPanel` → `BoundComboBox` substitution for `FM_DESTINATION` (`MainComponent` control factory) with a real **exclusive two-radio group** (`VCO1`=0 / `VCF`=1), reusing the existing exclusive-toggle pattern (`PageSelectorButton` + `setClickingTogglesState(true)` + `setRadioGroupId(...)`), bound to the `FM_DESTINATION` parameter; VFD label feedback unchanged (RQ-GUI-020). Radio appearance entirely from design-system tokens (no raw visual literal); layout must fit the reserved `RadioButtonPanel` bounds (191,189,82,42). **Status: Done** — new `BoundRadioGroup` (IBoundControl container of exclusive `juce::ToggleButton`s, shared radio group id); `XplorerLookAndFeel::drawToggleButton` branches on `getRadioGroupId() != 0` to draw a circular `drawRadioBox` (surfaceBase fill, LED-accent border/dot, tokens only) instead of the square tick box, so check boxes are untouched; `MainComponent` builds the radio group only for `FM_DESTINATION` (the LAG `LINEAR/EXPO` panel keeps its combo — scope-limited to RQ-GUI-038). Rebuilt (XplorerApp, exit 0); verified under Xvfb: two circular radios `VCO1`/`VCF` with one selected, checkboxes still square, clicking `VCF` selects it + deselects `VCO1` + the VFD shows `FM DESTINATION:VCF`. *Acceptance (Gherkin):* Given the FM/VCA block, When shown, Then two radios `VCO1`/`VCF` (not a combo) are visible with exactly one selected; When `VCF` clicked, Then `FM_DESTINATION`=1 transmitted, `VCO1` deselects, VFD shows `VCF`; Given an incoming change sets `FM_DESTINATION`=0, Then `VCO1` becomes selected. | M | RQ-GUI-038; ADR-JUC-016 (DEC-JUC-016), ADR-JUC-014 |
| TASK-JUC-106 | **Active FM signal-path overlay.** Was: a mouse-transparent `FmSignalPathOverlay` lighting the branch routing to the selected `FM_DESTINATION` in the LED accent, geometry single-sourced with `BackgroundRenderer`. **Status: Withdrawn (owner decision 2026-07-23 — "les radio buttons sont bien suffisants").** Implemented and verified under Xvfb (commit `e84c24a`), then fully reverted at the owner's request (revert of `e84c24a`): `FmSignalPathOverlay.{hpp,cpp}` and `FmSignalPath.hpp` deleted, `BackgroundRenderer` restored to its inline FM-branch lines, `MainComponent` overlay wiring removed. The FM destination two-way radio (TASK-JUC-105) is kept and is considered sufficient for the routing legibility goal. RQ-GUI-039 withdrawn accordingly. | M | RQ-GUI-039 (withdrawn); ADR-JUC-016 (DEC-JUC-017/018/019 withdrawn) |
| TASK-JUC-108 | **Radio-row vertical alignment fix** (added 2026-07-23, owner review of TASK-JUC-107). The LAG radios sat on a different vertical rhythm than the sibling `EQUAL TIME`/`LEGATO` check boxes: `BoundRadioGroup::resized()` even-split the 47 px panel into 23 px rows and let each button fill its row, so the circular radio (centred in a 23 px button) landed ~3 px below the 17 px check boxes on the lower row. Fix: new design-system token `controlRowHeight` (17, the reference control-row height shared by check boxes/radios) added to `design-tokens.yaml` (global + semantic), `DesignTokens.hpp` regenerated (`--check` clean); `BoundRadioGroup::resized()` now sizes each option to `controlRowHeight` at the top of its slot (`removeFromTop(slot).withHeight(controlRowHeight)`) instead of filling the slot, so radio rows align with the sibling check-box rows. **Status: Done** — rebuilt (XplorerApp, exit 0); verified under Xvfb: `LINEAR`↔`EQUAL TIME` and `EXPO`↔`LEGATO` now share their rows; FM destination panel (same code path, no sibling check boxes) unchanged in appearance. *Acceptance (Gherkin):* Given the LAG block, When rendered, Then the `EXPO` radio and the `LEGATO` check box sit on the same horizontal line (and `LINEAR`/`EQUAL TIME` likewise). | M | RQ-GUI-040, RQ-DSN-020; ADR-JUC-014 |
| TASK-JUC-107 | **LAG timing two-way radio selector** (added 2026-07-23, owner request — the reference had it as radios). Apply the exact TASK-JUC-105 pattern to the second fixed-block `RadioButtonPanel`, `LAG_TIMING_LINEAR_EXPO` (`LINEAR`=0 / `EXPO`=1, `rdLinear`/`rdExpo`): generalised the `MainComponent` control factory so **every** fixed-block `RadioButtonPanel` renders as a `BoundRadioGroup` (removed the FM-only special case and the now-unused `FM_DESTINATION_TAG`); no new widget/skin code (reuses `BoundRadioGroup` + `XplorerLookAndFeel::drawRadioBox`). Scope: only the two fixed-block panels (FM, LAG) reach this factory; the `ENV_X`/`RAMP_X` trig panels are page-family controls handled elsewhere and are untouched. **Status: Done** — rebuilt (XplorerApp, exit 0); verified under Xvfb: LAG block shows circular radios `LINEAR`/`EXPO` (combo gone), `EQUAL TIME`/`LEGATO` still square check boxes and the `KEYBD` LAG-IN combo intact; clicking `EXPO` selects it + deselects `LINEAR` + the VFD shows `TIMING:EXPO`. *Acceptance (Gherkin):* Given the LAG block, When shown, Then two radios `LINEAR`/`EXPO` (not a combo) with one selected; When `EXPO` clicked, Then `LAG_TIMING_LINEAR_EXPO`=1 transmitted, `LINEAR` deselects, VFD shows `EXPO`; Given an incoming change sets the value 0, Then `LINEAR` becomes selected. | S | RQ-GUI-040; ADR-JUC-016 (DEC-JUC-016), ADR-JUC-014 |

### Control interaction states — hover / focus / disabled (RQ-GUI-041/042/043, ADR-JUC-017)

Added 2026-07-23, owner-requested (UX review item #1: "l'IHM parait morte sous la souris"). Split
along the ADR's decision boundaries (DEC-JUC-020..024) so each task is independently verifiable
under Xvfb and independently revertable. `session.unit_tests = false` → no unit tests; functional
verification via the Xvfb/screenshot pipeline per this session's established rule. Presented for
DoR approval before starting.

| Task | Description | Tier | RQ / ADR |
|------|-------------|:-:|---|
| TASK-JUC-109 | **Hover, disabled and focus for check boxes and radio buttons.** New `component.disabledAlpha = 0.5` token (`design-tokens.yaml`, global + semantic alias, regenerate `DesignTokens.hpp`, `--check` clean). `XplorerLookAndFeel::drawToggleButton` stops discarding `shouldDrawButtonAsHighlighted`/`isEnabled` and forwards them into `drawTickBox`/`drawRadioBox`; both apply `component.hoverBrighten` to the accent-derived colours when highlighted and `component.disabledAlpha` to the whole box when `!isEnabled`; both additionally stroke a `ledColour()` outline at `semantic.strokeLine` width around the control's outer bounds when `component.hasKeyboardFocus(true)`, drawn on top of the idle/hover/disabled fill. Covers `BoundCheckBox` and every `BoundRadioGroup` option (FM destination, LAG timing) via the one shared code path. **Status: Done** — token added (`--check` clean), rebuilt (exit 0); verified under Xvfb with numeric pixel-mean sampling of the KEYB tick box (away 0.232 → hover 0.240, border brighten; → focus 0.334, accent ring with the pointer elsewhere — pure keyboard-focus state, visually distinct from hover). Disabled state not functionally testable (no `setEnabled(false)` call site exists — readiness work per RQ-GUI-043's own text); it shares the same alpha-multiplier path as the two verified states. *Acceptance (Gherkin):* Given an enabled check box or radio, When the pointer enters it, Then it brightens by `hoverBrighten` and reverts on pointer-exit; Given the same control with `setEnabled(false)`, When rendered, Then it shows at `disabledAlpha` and does not hover-brighten; Given the control has keyboard focus, When rendered, Then an accent-coloured outline ring is drawn around it, distinguishable from the hover/disabled fill; Given the FM/LAG radio panels, When rebuilt, Then their appearance and behaviour from TASK-JUC-105/107/108 are otherwise unchanged. **Focus half undone 2026-08-04 by PLAN-GFX-001 / TASK-GFX-001 (RQ-GUI-054, ADR-JUC-029): the accent ring is removed from `drawTickBox`/`drawRadioBox`. Hover and `disabledAlpha` stand.** | M | RQ-GUI-041, RQ-GUI-042, RQ-GUI-043; ADR-JUC-017 (DEC-JUC-020, DEC-JUC-022, DEC-JUC-024), ADR-JUC-011, ADR-JUC-014 |
| TASK-JUC-110 | **Hover, disabled and focus for combo boxes.** New `XplorerLookAndFeel::drawComboBox` override (today's combos fall back to `LookAndFeel_V4`'s untokenised default) reading `box.isMouseOverOrDragging()`/`box.isEnabled()`/`box.hasKeyboardFocus(true)` directly from the `juce::ComboBox&` parameter, applying `hoverBrighten`/`disabledAlpha` to the existing `surfaceRecessed` fill and the same accent focus-ring rule as TASK-JUC-109 — arrow, popup styling and the existing per-box font-shrink logic (`getComboBoxFont`, RQ-GUI-032) untouched. The combo corner unifies to the shared `radiusControl` (2 px, was JUCE's default 3 px) — a deliberate 1 px design-system-consistency change, not a regression. **Scope note (found during implementation):** `juce::ComboBox` does **not** repaint on plain hover (unlike `Slider`), so the LookAndFeel's hover state is only redrawn when the combo repaints; `BoundComboBox` (all main-panel/page-family combos) therefore gains `mouseEnter`/`mouseExit → repaint()`. The modulation-matrix combos are plain `juce::ComboBox` with their own dedicated RQ-GUI-018 highlight mechanism and are intentionally left to it (no hover-repaint added there); disabled/focus still render through the shared `drawComboBox` for every combo. **Status: Done** — rebuilt (exit 0); verified under Xvfb by numeric fill-strip pixel-mean of the LAG-IN combo: away 0.144 → hover 0.387 (clear brighten), reverting on exit; focus ring and disabled reuse the exact code path verified in TASK-JUC-109. **Focus half undone 2026-08-04 by PLAN-GFX-001 / TASK-GFX-001 (RQ-GUI-054, ADR-JUC-029): `drawComboBox` no longer reads `hasKeyboardFocus`. Hover, disabled, arrow and font logic stand.** | M | RQ-GUI-041, RQ-GUI-042, RQ-GUI-043; ADR-JUC-017 (DEC-JUC-021, DEC-JUC-022, DEC-JUC-024), ADR-JUC-014 |
| TASK-JUC-111 | **Hover for the Settings MIDI-automation table rows.** `TableListBoxModel` has no built-in per-row hover callback (verified against the JUCE header); `SettingsDialog`'s `AutomationTableModel` gains a `_hoveredRow` member and a `MouseListener` attached to its `juce::TableListBox` (`mouseMove`/`mouseExit`, row resolved via `TableListBox::getRowContainingPosition`), repainting only the previously/newly hovered rows; `paintRowBackground` applies `hoverBrighten` when `rowNumber == _hoveredRow && !isRowSelected` — selection keeps visual precedence over hover (RQ-DSN-062 ordering). Implemented as a `TableHoverListener` (struct : `juce::MouseListener`) attached to `_automationTable` with nested=true (so events over the CC combos count); `rowAt` uses `MouseEvent::getEventRelativeTo(&table)` + `ListBox::getRowContainingPosition`, and only the previously/newly hovered rows are `repaintRow`'d. **Status: Done** — rebuilt (exit 0); verified under Xvfb (Tools→Settings→MIDI): hovering the `VCO1 DETUNE` row brightened its background (numeric row-bg pixel-mean 0.144 → 0.387) while `VCO1 FREQ`/`VCO1 PW` stayed neutral — single-row hover confirmed. | M | RQ-GUI-041; ADR-JUC-017 (DEC-JUC-023) |

### Functional-block identity colours (RQ-GUI-044, RQ-DSN-092/093/094, ADR-JUC-018)

Added 2026-07-25, owner request (GUI-modernisation study). Mockup-first per ADR-JUC-013:
TASK-GUI-001/002 are the validated SVG stage, TASK-GUI-003 ports it to the painter.
`session.unit_tests = false` -> functional verification by mockup-vs-app pixel comparison.

| Task | Description | Tier | RQ / ADR |
|------|-------------|:-:|---|
| TASK-GUI-001 | **Block palette + coloured frames/sections (mockup).** 8 block hues as global+semantic tokens, `sectionBarFadeEnd`; `generate_background_mockup.py` gains `box(..,c)` / `section(..,blk)` and one bar gradient per block; SVG regenerated. **Status: Done** — token `--check` clean; SVG geometry diff (colour attributes stripped) = 0 changed lines, only the 8 new `<linearGradient>` defs added. | M | RQ-GUI-044, RQ-DSN-092/093; ADR-JUC-013, ADR-JUC-014 |
| TASK-GUI-002 | **Block fill + frame relief (mockup).** Measured from the owner-supplied reference screenshot: fill `#443326` over plate `#242528` for block `#C27A52` -> `blockFillAlpha` 0.18; frame top `#8D4E1C` vs bottom `#75421C` (ratio 0.84) -> `blockFrameRelief` 0.2 (`darker()` amount). `box()` fills with the hue at alpha and strokes a top-bright/bottom-dark gradient. Control sub-panels (ENV/RAMP trigger, FM DESTINATION) reclassified to **neutral, unfilled** after finding them grey (`#444748`, untinted interior) in the same reference. **Status: Done** — owner-accepted ("c'est juste excellent"). | M | RQ-GUI-044, RQ-DSN-094; ADR-JUC-013, ADR-JUC-014 |
| TASK-GUI-003 | **Port the block colours to `BackgroundRenderer.cpp`.** Hand-port the validated mockup 1:1 (ADR-JUC-013): `box()` gains an optional block-colour argument (fill at `blockFillAlpha` + `ColourGradient` frame to `darker(blockFrameRelief)`); `section()` gains a block colour for its label and its bar gradient (hue -> hue at `sectionBarFadeEnd`), replacing the shared blue bar; the ~25 block call sites are tagged with their semantic token. Neutral default leaves control sub-panels and every connector line untouched. No geometry, no control, no layout change. Unused `sectionBarTop/Mid/Bot` tokens are **kept** (add-only governance). **Status: Done** — 27 call sites tagged (19 labelled boxes + 8 sections), rebuilt (exit 0). Verified per DEC-JUC-028: app vs mockup sampled at the same points agree within 1/255 (ENV fill `#524645`/`#524645`, ENV frame bottom `#A16544`/`#A26644`, VCO fill `#485060`/`#485060`, LFO fill `#424153`/`#424254`, plate and signal line identical); geometry proven unchanged by diffing the painter — every `box()`/`section()` line is byte-identical once the added block argument is stripped, so no coordinate moved. *Acceptance (Gherkin):* Given the rebuilt app, When the panel is painted, Then each block's interior, frame and section header show its palette hue and the frame's top edge is brighter than its bottom edge; Given the same build, When app and mockup are sampled at the same block interiors/edges, Then the measured colours agree with the token-derived expectations; Given the areas outside the blocks (lines, sub-panels, plate, rails, controls), When compared with the pre-change screenshot, Then they are unchanged. | M | RQ-GUI-044, RQ-DSN-092/093/094; ADR-JUC-018 (DEC-JUC-025..028), ADR-JUC-013, ADR-JUC-014 |

### Design-system backlog (phase 2 — owner-reasoned, NOT value-preserving)

| Task | Description | Tier | RQ / ADR |
|------|-------------|:-:|---|
| TASK-JUC-099 | Font-size consolidation (e.g. 13.5 vs 13) — changes pixels, owner sign-off. | M | RQ-DSN-010 |
| TASK-JUC-100 | Missing interaction states: shared hover/disabled/focus for toggle/combo/list rows. | M | RQ-DSN-030..033 |
| TASK-JUC-101 | Accessibility gaps: colour-alone LED/highlight meaning, contrast audit, focus ring. | M | RQ-DSN-050..052 |