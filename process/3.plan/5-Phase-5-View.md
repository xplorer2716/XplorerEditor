## Phase 5 — View (JUCE app)

> Refined 2026-07 after owner review of the reference UI (screenshot + description; ADR-JUC-006).
> Order: assets → shell → binding → blocks → matrix → display → menus/dialogs → skin.
> UI logic (registry, family substitution, matrix glue) is headless-tested; visual validation on Windows by the owner at each milestone (M1 = after 064, M2 = after 067, M3 = after 069).

| Task | Deliverable | Tier | Requirements |
|---|---|---|---|
| TASK-JUC-060 | Asset & layout extraction: background PNG + button GIFs out of `MainForm.resx`; mechanical extraction of the 234-control table (id, type, bounds, tag) from `MainForm.Designer.cs` into a declarative C++ table | M | RQ-GUI-007, RQ-GUI-001 |
| TASK-JUC-061 | App shell: JUCE app target (`XPL_BUILD_APP`), main window with logical-canvas scaling + background bitmap, `JuceEventDispatcher`, single-instance guard, splash *(removed 2026-08-04, PLAN-GFX-002 / TASK-GFX-004, RQ-GUI-055, ADR-JUC-030)*, top-level exception dialog | L | RQ-GUI-005..007, RQ-GUI-035 |
| TASK-JUC-062 | `ParameterBindingRegistry`: id→parameter wiring, anti-echo guard, automation-disable on edit, full-tone refresh — headless tests against mock backend | L | RQ-GUI-002..004 |
| TASK-JUC-063 | Fixed blocks (VCO1, VCO2, FM, LAG, VCF/VCA): standard JUCE controls placed from the extracted table, bound via registry | L | RQ-GUI-001, RQ-GUI-030, RQ-GUI-032 |
| TASK-JUC-064 | `PageFamilyBlock` ×4 (ENV/LFO/RAMP/TRACK): shared controls, selector buttons, digit substitution, synth page-change sync — logic headless-tested. **Milestone M1: owner Windows build** | L | RQ-GUI-010..012 |
| TASK-JUC-065 | Modulation matrix panel: 20 rows, availability rules, entry/full-tone refresh | L | RQ-GUI-015..017 |
| TASK-JUC-066 | Display panel (fixed-font text: program, name, last parameter, typewriter) + 8 shortcut buttons + MIDI activity indicator. *M2 owner review (#9): `VfdDisplayHelper` ported so the last-parameter line shows the friendly name + value-by-control-type (combo/radio label, checkbox Y/N, knob numeric) + MIDI CC, and modulation-matrix edits display SRC/DEST/AMNT/QTZ; the `.` active-mod-destination marker is deferred.* | L | RQ-GUI-020..022 |
| TASK-JUC-067 | Menu bar + dialogs: settings (3 pages: MIDI/UI/randomizer), rename, store/goto, extract single tones, backup/restore + get-all-patches with progress, about; window-wide `.syx` drag & drop + shared load-by-type. **Milestone M2**. *Morphing deferred: the reference `ToneMorphingForm` is flagged work-in-progress (empty OK/Cancel, not wired to any menu) — see architecture-analysis_juce §11.* | L | RQ-GUI-008, RQ-GUI-025..027, RQ-GUI-029 |
| TASK-JUC-068 | Piano-keyboard window | M | RQ-GUI-028 |
| TASK-JUC-069 | Skin pass: `XplorerLookAndFeel` (knobs + LED ring per UiConfiguration, image buttons, custom checks/radios). **Milestone M3**. *The VFD rendering piece is split out into TASK-JUC-073.* | L | RQ-GUI-031 |
| TASK-JUC-073 | VFD bitmap glyph rendering (added 2026-07 after M2 review): extract `MATRIXTINY` sheet to assets, `DisplayPanel` paints 12×16 glyph cells per ADR-JUC-007 (direct sprite-sheet paint + `setBufferedToImage`, reference grid formula, nearest-neighbour scaling); `VfdDisplayHelper` wrap width unified with the computed grid; display grown to 5 rows (82 px, upward) per the owner-arbitrated option (b). | L | RQ-GUI-033 |
| TASK-JUC-074 | MIDI LED panel (added 2026-07 after M2 review): replace the single activity LED by the reference 3-LED `LedPanelControl` port — green automation-in / blue synth-in / red synth-out, reference geometry and colours, ≈100 ms retriggerable hold, event-driven timer per ADR-JUC-008. | M | RQ-GUI-022 |
| TASK-JUC-075 | Rotary knob cosmetics + page-selector captions (added 2026-07 after M2/M3 review): selector buttons show `ENV 1`…/`TRACK 3` (extracted `.label`, instance tracked separately from the caption); rotary rendered as LED-ring-only (no centre pointer) with a hover-brightened ring; knob text box removed → value as a drag popup bubble + double-click inline numeric entry, rotary fills its full bounds (larger). Per ADR-JUC-009. **Status: Done** — delivered in commit `ca38cba` ("TASK-JUCE-075/076: knob & page-selector cosmetics + modulation-matrix highlight"); verified in code: selector captions carry the extracted `.label` (`"ENV 1"`…), `drawRotarySlider` draws the LED ring with no centre pointer, and `BoundKnob` uses `NoTextBox` plus the double-click inline numeric entry. *(Status corrected 2026-07-27 during the traceability audit: the entry still read "Awaiting owner go-ahead"; the commit's `TASK-JUCE-075` typo hid it from ID greps.)* | M | RQ-GUI-011, RQ-GUI-031, RQ-GUI-034 |
| TASK-JUC-076 | Modulation-matrix hover highlight + VFD active-destination marker (added 2026-07 after M2/M3 review): headless `ModulationDestinationResolver` + page-selector→source map (`xpl_app_core`, unit-tested); `ModMatrixPanel` highlight API; mouse enter/exit wiring from selectors and knobs; and the deferred VFD `.` marker resolved with the same resolver. Per ADR-JUC-010 (closes analysis gap §10-12). | L | RQ-GUI-018, RQ-GUI-020 |
| TASK-JUC-077 | LED-colour single source of truth (added 2026-07 after M3 review): the LookAndFeel becomes the sole runtime owner of the knob LED colour (`ledColour()` accessor); `ModMatrixPanel` derives the highlight colour from it instead of caching a copy, so changing the colour in the settings updates the matrix highlight live. Removes the duplicated colour state. Per ADR-JUC-011. | M | RQ-GUI-018, RQ-GUI-031 |
| TASK-JUC-078 | MIDI CC automation table — load path + editor (added 2026-07 after M3 review): (1) generate `GeneratedControlChangeNames.inc` (129 CC names) from the reference; (2) load the persisted `"NAME;CC"` table into the controller dictionary on startup/settings-apply (headless-tested parser) — currently missing, so CC automation and the VFD CC line do not work; (3) add the parameter↔CC mapping editor (list + per-row CC combo + reset) to the MIDI settings page; (4) an "Export as HTML" action writing a compact light print-friendly mapping document (headless `buildMidiMappingHtml`, unit-tested) opened in the browser. Per ADR-JUC-012. | L | RQ-GUI-036, RQ-SET-002 |
| TASK-GUI-004 | **Block colour identity on the page-family selector buttons** (added 2026-07-26, owner request). `PageSelectorButton` overrides `paintButton` to draw its own background: border always the pure block hue, background = `blockColour.withAlpha(blockFillAlpha)` (the block's own fill) only while the instance is active, neutral otherwise; hover (`hoverBrighten`) and keyboard-focus (accent ring) reproduced so RQ-GUI-041/042 do not regress; caption still drawn by `LookAndFeel::drawButtonText`. `PageFamilyBlock`'s constructor maps `descriptor.controlTagPrefix` (`ENV_X`/`LFO_X`/`RAMP_X`/`TRACK_X`) to the matching `tokens::semantic::block*` and injects the colour into each button. No new design token, no geometry/caption/behaviour change, no `LookAndFeel` change (other `TextButton`s untouched). **Status: Done** — rebuilt (XplorerApp, exit 0); verified under Xvfb per DEC-JUC-033 by pixel sampling: each family's active selector background matches the fill sampled inside its own painted block within 1/255 (ENV `#534746`/`#524645`, LFO `#434254`/`#424153`, RAMP `#3A464E`/`#3A454D`, TRACK `#4A4540`/`#4A453F`), inactive buttons stay neutral plate colours, and every button of a row is outlined in its block hue. *(Sampling note: selector buttons sit in control-table coordinates while the diagram is translated by -27, and centred captions must be avoided — first-pass samples that ignored both read the plate or the label text.)* *Acceptance (Gherkin, per RQ-GUI-045):* Given a selector row, When rendered, Then every button is outlined in its block's hue; Given the active instance, When rendered, Then its background matches the fill sampled inside that block's own frame and the other buttons of the row are neutral; Given another instance is selected, Then the coloured background follows it; Given a hovered or focused selector, Then the RQ-GUI-041/042 feedback is still visible. **Focus half undone 2026-08-04 by PLAN-GFX-001 / TASK-GFX-002 (RQ-GUI-054, ADR-JUC-029): `paintButton` no longer draws a focus ring; `setWantsKeyboardFocus(true)` stays for the Ctrl+C/Ctrl+V gesture. Block border, active fill and hover stand.** | M | RQ-GUI-045, RQ-GUI-041, RQ-GUI-042; ADR-JUC-019 (DEC-JUC-029..033), ADR-JUC-018, ADR-JUC-014 |
| TASK-GUI-005 | **Palette v2 as the default block palette** (added 2026-07-26, owner "option B"). `design-tokens.yaml` block* hex values updated to the owner-selected v2 set (blockVco `549ECB`, blockLag `8F969D`, blockTrack `B19047`, blockVcf `7AA364`, blockEnv `C88160`, blockLfo `A087C9`, blockRamp `37A9A3`, blockMatrix `6579EE` — methodology and metrics recorded in RQ-DSN §2.1b); `DesignTokens.hpp` regenerated (`--check` clean) and `background-mockup.svg` regenerated. No code change — every consumer reads the tokens. **Status: Done** — rebuilt (XplorerApp, exit 0); verified under Xvfb by sampling the eight section bars at their label end (pure hue): every sample within 4/255 of its v2 token (gradient-fade onset at the sample point), e.g. VCO `#539CC8`/`549ECB`, MATRIX `#6377EA`/`6579EE`. *Acceptance:* Given the regenerated tokens, When the app is built and rendered, Then each painted block carries its v2 hue (pixel-sampled fill = hue at `blockFillAlpha` over the plate). | S | RQ-DSN-092; ADR-JUC-020, ADR-JUC-014 |
| TASK-GUI-006 | **`BlockPalette` runtime plumbing** (added 2026-07-26). New `BlockPalette.hpp/.cpp`: struct with eight named `juce::Colour` fields + `defaultBlockPalette()` (DEC-JUC-034), `BlockId` enum + static descriptor table `{id, display name, member pointer}` (DEC-JUC-035), `resolveBlockPalette(uiConfig)` (override ?? default). `XplorerLookAndFeel` gains `blockPalette()`/`setBlockPalette()` (DEC-JUC-036); `paintVectorBackground` takes `const BlockPalette&` (pure function; call sites `MainComponent::paint` = live palette, splash = defaults); `PageSelectorButton` stores its `BlockId` and resolves the palette at paint time (DEC-JUC-037, removes the DEC-JUC-030 cached colour); `MainComponent::updateLedColour` carries the palette across the LookAndFeel rebuild. **Status: Done** — rebuilt (XplorerApp, exit 0); verified under Xvfb: full-screen pixel diff against the TASK-GUI-005 capture is empty (byte-identical rendering with no overrides); the override-propagation path is pixel-verified in TASK-GUI-007. *Acceptance:* Given no overrides, When rendered, Then output is identical to TASK-GUI-005's; Given `setBlockPalette` with a distinct hue, When the tree repaints, Then the painted block AND its selector buttons follow with no stale copy. | M | RQ-DSN-095, RQ-GUI-045; ADR-JUC-020 (DEC-JUC-034..037) |
| TASK-GUI-007 | **Per-block override persistence** (added 2026-07-26). `UiConfiguration` gains `std::array<std::optional<int>, BLOCK_COLOUR_COUNT> blockColours` (ARGB, index order = `BlockId` order); `XmlSettingsService` writes only set entries (`BlockColorVco`…`BlockColorMatrix`) and treats missing elements as unset, so pre-existing and imported .NET files load unchanged (DEC-JUC-039); startup resolves override ?? default into the LookAndFeel palette. **Status: Done** — rebuilt (XplorerApp, exit 0); verified under Xvfb: with a hand-injected `<BlockColorEnv>-65536</BlockColorEnv>` (pure red) the ENV frame top samples `#FE0000`, the ENV section bar `#FB0000` and the ENV 1 selector border `#FF0000` with a red-tinted active fill, while VCO stays at its v2 default `#539CC8`; without block elements the rendering is byte-identical to defaults (TASK-GUI-006 diff). *Acceptance:* Given a settings file without block entries, When loaded, Then all overrides are unset and defaults render; Given a file with one `BlockColorEnv` entry, When loaded, Then only ENV renders the override; Given cleared optionals, When saved, Then no block element is written. | M | RQ-SET-007, RQ-SET-006; ADR-JUC-020 (DEC-JUC-039) |
| TASK-GUI-008 | **Settings UI: COLOURS group + Reset to defaults** (added 2026-07-26, owner-validated mockup). `UiSettingsPage` restructured into a **COLOURS** group (unity hint line; knob LED row; separator + block hint; 2×4 grid VCO/FM, LAG, TRACK, VCF/VCA | ENV, LFO, RAMP, MOD MATRIX — each label + swatch + uniform "Choose..." button opening a `ColourSelector` call-out; **Reset to defaults** inside the group restoring the 8 blocks + knob LED as one set) and a **KNOB BEHAVIOUR** group (movement/style radios). Live preview via `setBlockPalette()` + `sendLookAndFeelChange()` (DEC-JUC-038); palette snapshot on open, restored on cancel/close; on accept overrides persist per DEC-JUC-039 (a colour equal to its default stores no entry; Reset clears all). **Status: Done** — rebuilt (XplorerApp, exit 0); verified under Xvfb by driving the dialog with xdotool + pixel sampling: (1) layout matches the validated mockup (COLOURS group with both hints, LED row, separator, 2×4 grid, uniform "Choose...", Reset inside the group; KNOB BEHAVIOUR group); (2) picking a TRACK colour in the call-out live-previews the panel behind the dialog (`#E717A7` on the TRACK section bar); (3) Cancel restores the snapshot (`#AF8E46` back) and persists nothing; (4) OK persists exactly one `<BlockColorTrack>` entry and keeps the colour live; (5) Reset to defaults restores LED `#66B5E3` + all block swatches and accepting writes zero block entries; (6) with no overrides the full-screen render is byte-identical to the TASK-GUI-006 capture. New layout/hint tokens added (dialogSwatchWidth/ChooseWidth/BlockLabelWidth/ResetWidth/GroupHeaderHeight, textHint) — no raw visual literal. *Acceptance:* Given the dialog, When a block colour is picked, Then the panel and its selectors preview live; Given Cancel, Then the palette snapshot is restored and nothing persists; Given OK, Then overrides persist and survive restart; Given Reset to defaults, Then all 8 blocks + knob LED show the defaults and accepting stores no block entries. | L | RQ-GUI-046, RQ-DSN-095, RQ-SET-007; ADR-JUC-020 (DEC-JUC-038/039) |
| TASK-GUI-009 | **VCO1/VCO2 MOD checkbox column alignment** (added 2026-07-26, owner request). `GeneratedControlTable.inc`: the `VCO2_MOD_{KEYB,LAG,LEV1,VIB}` checkboxes were 10 px left of their `VCO1_MOD_*` counterparts (same widths, unaligned columns) — shifted +10 px to match VCO1's x positions exactly; `VCO2_WAVE_SYNC` (VCO2-only, no VCO1 counterpart) shifted the same +10 px to keep its gap after VIB, and the `VCO2_MODS` FakePanel backdrop widened +10 px to keep the same trailing margin. Deviation from the archived .NET reference recorded inline with rationale (table is now the source of truth, RQ-DSN deviation-recording rule). **Status: Done** — rebuilt (XplorerApp, exit 0); verified under Xvfb: KEYB/LAG/LEV1/VIB x-coordinates now identical between the VCO1 and VCO2 rows (154/213/265/322), confirmed by screenshot. | S | RQ-GUI-001 |
| TASK-GUI-010 | **Coloured block-border continuity where knob stubs/signal lines touch an edge** (added 2026-07-27, owner request). `BackgroundRenderer.cpp`: a stub/line's rounded end-cap, drawn after the block in mockup order, notched the neutral FRAME colour over the coloured border wherever a knob tick meets a block's edge (e.g. VCO1 DETUNE). `box()` now records `{x,y,w,h,block}` for every coloured block into `colouredBorders`; a final pass at the end of `paintVectorBackground` re-strokes just those borders (same relief gradient, fill untouched) on top of everything else, so the border is always the last thing painted. Neutral (uncoloured) boxes are unaffected (their FRAME border already matches the stub colour). **Status: Done** — rebuilt (XplorerApp, exit 0); verified under Xvfb: pixel diff against the TASK-GUI-009 capture shows 529 changed pixels, every one at a stub/line-to-border junction across all 8 blocks; zoomed capture at the VCO1/DETUNE junction confirms the border line is now unbroken where the tick meets it (no more white notch). | S | RQ-GUI-037; ADR-JUC-018 |
| TASK-HLT-001 | **Mod-matrix combobox hover state goes stale on fast mouse movement (GitHub issue #21)** (added 2026-07-27). Root cause: `juce::ComboBox` never repaints itself on `mouseEnter`/`mouseExit` (unlike `Slider`/`Button`), so `XplorerLookAndFeel::drawComboBox`'s live `isMouseOverOrDragging()` read (DEC-JUC-021) only shows correctly when *some* repaint happens to occur — sweeping the mouse fast down the mod matrix's 20-row source/destination column left several combos stuck showing their last-painted hovered/brightened frame. `BoundComboBox` already had the two-line fix (`mouseEnter`/`mouseExit` → `repaint()`) for parameter-bound combos; the mod-matrix combos are plain `juce::ComboBox` (composite controller operations, not a named parameter, so never routed through `BoundComboBox` — ADR-JUC-017 DEC-JUC-040). Unified rather than duplicated: new `HoverRepaintingComboBox.hpp` (single `juce::ComboBox` subclass, no `ParameterBindingRegistry` dependency); `BoundComboBox` now inherits from it instead of `juce::ComboBox` directly (its own duplicate overrides removed); `ModMatrixPanel::buildRow` constructs `row.source`/`row.destination` as `HoverRepaintingComboBox` instead of raw `juce::ComboBox`. No behaviour change to parameter-bound combos (drop-in base swap, constructor unaffected). **No unit test**: pure `juce::Component` mouse-event/repaint plumbing with no headless-testable logic; no class under `juce/app/src/` has Catch2 coverage in this repo (only `app/core` does) — verification is visual, per the established convention for this layer. **Status: Done** — rebuilt (XplorerApp, exit 0), full suite green (81/81 passed, 1 environment-dependent MIDI-cable scenario skipped). Verified under Xvfb by driving the pointer with xdotool and sampling each combo's fill (mode of its interior pixels): with the pointer on MOD_SRC_1 it reads `#5E6268` (idle `#1E242C` brightened by `hoverBrighten`) and MOD_SRC_10 reads `#1E242C`; after moving the pointer to MOD_SRC_10, MOD_SRC_1 correctly returns to `#1E242C` and MOD_SRC_10 becomes `#5E6268`. **The check was proven to discriminate**: temporarily reverting the two `HoverRepaintingComboBox` constructions to raw `juce::ComboBox` and rebuilding reproduces the issue exactly — MOD_SRC_1 stays stuck at `#5E6268` after the pointer leaves *and* MOD_SRC_10 never lights up at all (`#1E242C`), since neither transition repaints; the fix was then restored and rebuilt. Non-regression on the parameter-bound path (whose base class changed): `LFO_X_WAVESHAPE` still reads `#5E6268` hovered and `#1E242C` idle. *Acceptance (Gherkin):* Given the mod-matrix source/destination column, When the pointer sweeps quickly down several combos, Then no combo the pointer has already left keeps showing a brightened/hovered background — only the one currently under the pointer does. Given a parameter-bound combo elsewhere in the panel (e.g. VCF mode), When hovered and left, Then its hover behaviour is unchanged from before this fix. | M | RQ-GUI-041; ADR-JUC-017 (DEC-JUC-021, DEC-JUC-040); GitHub issue #21 |
| TASK-CBL-001 | **Shared combo-box font-size policy, headless and unit-tested** (added 2026-07-27, issue #12 follow-up). New JUCE-free module in `xpl_app_core`: `collectComboBoxSizingInputs()` (walks `controlTable()` for `ComboBoxValuedControl` entries, pairs each id/width with its label set from `comboLabelsForControl()`) and `computeSharedComboBoxFontSize(inputs, baseSize, minSize, arrowZone, labelMargin, measureWidth)` returning `{sharedSize, idsBelowFloor}`, where `measureWidth` is an **injected** `std::function<float(std::string_view, float)>` so the module keeps `xpl_app_core`'s zero-JUCE-dependency status (ADR-JUC-002) and is covered by the always-run `linux-headless-release` CI job. Per-box candidate = today's `getComboBoxFont` formula; shared size = **minimum** of all candidates clamped to `[minSize, baseSize]`; `idsBelowFloor` = boxes whose **unclamped** candidate is below `minSize`. Catch2 tests in `xpl_tests_app` with a deterministic fake measurer (proportional-width stub): shared size equals the most-constrained box's candidate; a box that fits at base size does not lower it; clamping to the floor; `idsBelowFloor` reports exactly the boxes below the floor and is empty otherwise; the real inventory is non-empty and every entry resolves to a non-empty label set. **Status: Done, then SUPERSEDED by TASK-CBL-004/005 (ADR-JUC-022)** — `computeSharedComboBoxFontSize` is removed: with the size fixed by a token there is nothing to aggregate. `collectComboBoxSizingInputs()` and the injected-measurer split survive, repurposed as the verification predicate `comboBoxesOverflowingAt` (DEC-JUC-050), so the headless coverage this task established is kept rather than lost. Original delivery note: — `ComboBoxSizing.hpp/.cpp` added to `xpl_app_core` (+ `ComboBoxSizingTests.cpp` to `xpl_tests_app`); full suite green **84/84** (was 81; +3 scenarios, 216 assertions), builds clean with zero warnings under `-Wall -Wextra -Wpedantic -Werror`. **The JUCE-free guarantee (DEC-JUC-041) was verified explicitly, not assumed**: a fresh `cmake -S juce -B … -DCMAKE_BUILD_TYPE=Release` with `XPL_BUILD_APP` off — the exact `linux-headless` CI configuration — builds the module and runs all 84 tests with no `XplorerApp` artefact produced. *(Test-correction note: the "narrow box" scenario initially asserted a shared size of 8.0 with an empty below-floor list; the code correctly returned 9.0 (clamped) and reported the box, since a 20-char label in a 10-char box falls under the floor. The **expectation** was wrong — it claimed to exercise the sharing rule but had crossed into the fallback — so the fixture was retuned to 15 chars (candidate ≈ 10.67, above the floor) and an explicit `sharedSize > MIN_SIZE` assertion added; the production code was not touched.)* *Acceptance (Gherkin):* Given a set of sizing inputs with one narrow box, When the shared size is computed, Then it equals that box's own candidate and no other box raises it; Given a box whose widest label cannot fit at the floor, When computed, Then its id appears in `idsBelowFloor` and the shared size is still clamped at the floor; Given the real control table, When collected, Then every `ComboBoxValuedControl` entry appears exactly once with a non-empty label set. | M | RQ-GUI-047, RQ-DSN-011; ADR-JUC-021 (DEC-JUC-041/042/043) |
| TASK-CBL-002 | **`getComboBoxFont` becomes a cached adapter over the shared policy** (added 2026-07-27). `XplorerLookAndFeel::getComboBoxFont` stops sizing per instance: it calls `collectComboBoxSizingInputs()` once, passes a `measureWidth` backed by `juce::Font{juce::FontOptions{size}}.getStringWidthFloat(...)`, memoises the resulting size in a `mutable` member and returns it for **every** combo box thereafter (DEC-JUC-045). Size bounds stay `tokens::semantic::textDisplay` / `textDense` (RQ-DSN-011) — no raw literal. The now-unused `juce::ComboBox&` parameter is documented as intentional, together with the explicit "no resize listener, no invalidation-on-resize" rule (control bounds are logical; `ScaledCanvasComponent` only transforms the canvas) so a future reader does not restore per-instance sizing or add resize plumbing. This task also **records the empirical measurement**: the computed shared size and the resulting `idsBelowFloor` list are captured in the task's Status line, since ADR-JUC-021 deliberately left them open. **Status: Done, then SUPERSEDED by TASK-CBL-004 (ADR-JUC-022)** — the adapter is replaced by a plain token read; the measurement it produced is what condemned the approach and is preserved here as the evidence. Original delivery note: build clean, full suite green 84/84, rendering verified under Xvfb (all combo boxes drew at one size). **Measurement (real JUCE metrics, base `textDisplay`=16, floor `textDense`=9):** shared size = **9.0 — clamped at the floor** — with **4 boxes below floor**: `LFO_X_RETRIG_MODE` (65 px, widest `EXTRIG`, needs 7.50), `LAG_IN` / `LFO_X_SAMPLE_INPUT` / `TRACK_X_IN` (67 px, widest `TRACK1`, need 7.65). Full candidate ladder: 7.50 · 7.65 ×3 · **9.38 `MOD_DEST_*` ×20** (`ENV1 RELEASE`) · 11.17 `ENV_X`/`RAMP_X_TRIG_LFO_SOURCE` · 11.25 `VCF_MODE` · 11.79 `LFO_X_WAVESHAPE` · 16.00 `MOD_SRC_*` ×20. **Consequence the ADR could not foresee:** because `MOD_DEST_*` governs at 9.38, widening only the four below-floor boxes (TASK-CBL-003 as specified) lifts the shared size to just 9.38 — the whole panel would sit at the legibility floor, where `MOD_SRC_*` renders at 16 today. Comparative full-screen renders (today / floor-only / widen-`MOD_DEST`-too) were produced for owner arbitration; **TASK-CBL-003's widening scope is therefore pending that decision**. Until it lands, this task's shared size leaves those four boxes ellipsized — a known, transient intermediate state. *Acceptance (Gherkin):* Given the running app, When any two combo boxes of different label-set families are rendered, Then their item text uses the identical font size; Given the window is resized, When the panel re-renders, Then the shared size is not recomputed and the text scales with the canvas transform; Given no label anywhere, When rendered, Then none is ellipsized. | M | RQ-GUI-047, RQ-DSN-011; ADR-JUC-021 (DEC-JUC-041/045) |
| TASK-CBL-003 | **Widen the combo boxes that cannot fit at the legibility floor, reflowing their rows** (added 2026-07-27). *Conditional on TASK-CBL-002's measurement: if `idsBelowFloor` comes back empty this task is closed as Not Required, with the measured evidence recorded.* For each reported box, increase its width in `GeneratedControlTable.inc` just enough for its widest label to fit at the shared size — never shrinking the font further, never truncating (RQ-GUI-047) — and reposition the sibling controls of that box's row/block in the same edit so the row stays visually aligned rather than gaining a gap or an overlap (DEC-JUC-044), identified the same way TASK-GUI-009 identified the VCO1/VCO2 MOD row. A single inline deviation note per affected group records the rationale, per the AGNOS design-system deviation-recording rule. Verified under Xvfb by pixel sampling: the widened box's label renders in full, and the row's controls keep a consistent left/right alignment with their unmodified neighbours. *Acceptance (Gherkin):* Given a combo box reported below the floor, When the panel is rendered after widening, Then its widest label renders in full at the shared font size; Given that box's row, When rendered, Then its sibling controls remain aligned with no overlap and no new gap; Given the modified geometry, When the table is read, Then one inline deviation note covers the whole affected group. **Status: Withdrawn (2026-07-28)** — never started. With the fixed size and the embedded condensed face (ADR-JUC-022), every group's longest label fits the reference widths with ~10 px to spare, so no combo box is widened and no row is reflowed. The geometry deviation this task existed to perform is not needed. | M | RQ-GUI-047; ADR-JUC-021 (DEC-JUC-043/044) |
| TASK-CBL-004 | **Fixed combo-box text size on an embedded condensed typeface** (added 2026-07-28, replaces TASK-CBL-001/002/003). Design tokens gain `comboTextSize` (12), `comboArrowZone` (20) and `comboLabelBorder` (2) — the last two reclaiming the 40 px of arrow/label overhead `LookAndFeel_V4` sizes for far taller controls (DEC-JUC-047). `RobotoCondensed-Regular.ttf` + its Apache-2.0 licence ship as binary assets; `XplorerLookAndFeel` loads the face once and `getComboBoxFont` returns `Font{FontOptions{typeface}.withHeight(comboTextSize)}` for every box — **attached explicitly**, never via `setDefaultSansSerifTypeface`, which corrupts glyph mapping on the pinned JUCE (DEC-JUC-049, with the prohibition noted at the call site). New `positionComboBoxText` override reads the same two tokens as `drawComboBox` so arrow and text cannot drift. The aggregation machinery of TASK-CBL-001/002 (`computeSharedComboBoxFontSize`, the memoised size, `<optional>` member) is deleted. **No change to `GeneratedControlTable.inc`** — measured margins on the widest label of each group run from +9.9 px (`LAG_IN`/`TRACK_X_IN`/`LFO_X_SAMPLE_INPUT`) to +45.9 px (`MOD_SRC_*`), `VCF_MODE` +30.2 px. Scope is combo boxes only; all other text keeps the host font. **Status: Done** — build clean (only the pre-existing `drawToggleButton` deprecation warning remains), full suite green 84/84, rendered under Xvfb with every combo box drawing at one size in the embedded face and no label truncated. *(Implementation note: the Font must be built **from** the typeface — `FontOptions{typeface}.withHeight(...)` — not with a later `.withTypeface()`, which asserts in JUCE because it requires the name/style fields to still be empty; a first pass used the latter and tripped `juce_FontOptions.h:126` on every combo. Recorded at the call site.)* *Acceptance (Gherkin):* Given the main window, When rendered, Then every combo box uses the identical token size in the embedded face and no label is ellipsized; Given the longest label of any group, When displayed, Then it fits its combo box's unchanged reference width; Given any non-combo text, When rendered, Then it is unchanged from before this task. | M | RQ-GUI-047, RQ-DSN-096, RQ-DSN-011; ADR-JUC-022 (DEC-JUC-046/047/048/049) |
| TASK-CBL-005 | **Fit and group-width guards** (added 2026-07-28). `ComboBoxSizing` keeps `collectComboBoxSizingInputs()` and replaces `computeSharedComboBoxFontSize` with `comboBoxesOverflowingAt(inputs, size, arrowZone, labelBorder, measureWidth)` returning the ids whose widest label overflows — still JUCE-free with an injected measurer, so it stays covered by the always-run `linux-headless-release` job (DEC-JUC-050). Two consumers: (a) Catch2 tests — the predicate itself with a fake measurer, plus the **group invariant** that combo boxes sharing a value list share a width (`LAG_IN`/`TRACK_X_IN`/`LFO_X_SAMPLE_INPUT` = 67, the two trigger sources = 65, `MOD_SRC_*` = 103, `MOD_DEST_*` = 103), which RQ-GUI-047 otherwise states only in prose; (b) a **debug-only** start-up assertion in `XplorerLookAndFeel` naming any control whose widest label does not fit at the real embedded-face metrics, compiled out of release builds (RQ-GUI-048). Existing `ComboBoxSizingTests` scenarios for the removed aggregation are replaced, not merely deleted. **Status: Done** — 3 scenarios / 174 assertions, full suite green 84/84 headless. **Both acceptance directions of RQ-GUI-048 were exercised, not assumed**: a Debug build (`CMAKE_BUILD_TYPE=Debug`, since the default build defines `NDEBUG` and compiles the check out) starts clean with the shipped labels; injecting an overlong entry into `LFORetrigModes` makes it fail with `RQ-GUI-048: combo box 'LFO_X_RETRIG_MODE' cannot show its widest label at 12pt` plus the assertion, and the labels were then restored. Group invariant locked at the reference widths (67 / 65 / 103 / 103 / 127 / 100 / 65). **Additionally, and correcting an assumption**: `jassertfalse` only breaks under a debugger and otherwise merely logs, so the start-up assertion can never fail a CI run on its own — a "Debug CI job that launches the app" would have gone green with an overflowing label. The real gate is a new JUCE-linked test `xpl_tests_app_juce` (`ComboBoxRealMetricsTests.cpp`, built only when `XPL_BUILD_APP=ON`, precedent `xpl_tests_midi_juce`) that measures the shipped labels with the embedded face on whatever platform it runs, and a `windows-app-debug` workflow that builds Debug on Windows and runs it — covering both the per-platform metrics and the `#if JUCE_DEBUG` paths a Release build never compiles. Both directions exercised locally: passes clean (53 assertions, no display needed), and reports `LFO_X_RETRIG_MODE widest="AN OVERLONG TEST LABEL" needs 111.785 of 41 px` when a label is deliberately overrun. Suite now 85/85. *Acceptance (Gherkin):* Given a fake measurer and a box too narrow for its widest label, When the predicate runs, Then that box's id is returned and no other; Given the shipped table, When the group-invariant test runs, Then every value-list group has a single width; Given a development build and the shipped labels, When the app starts, Then no assertion fires; Given a label edited beyond its box, When a development build starts, Then the assertion fires naming that control. | M | RQ-GUI-048, RQ-GUI-047; ADR-JUC-022 (DEC-JUC-050) |
| TASK-GUI-014 | **Patch-number spinner replacing the Go to patch / Store 100-item combo** (added 2026-08-05, owner report, session GUI). `showStoreOrGotoDialog` (`Dialogs.cpp`) drops `AlertWindow::addComboBox` for a `juce::Slider(IncDecButtons, TextBoxLeft)` added via `addCustomComponent` — confirmed against the vendored JUCE source as the cross-platform equivalent of the native Windows spinner/`NumericUpDown` (stacked +/- buttons render automatically once the button zone is narrower than it is tall). `setRange(0, 99, 1)` gives native clamp-not-wrap behaviour (owner decision: a spinner pinned at a bound is the standard, unsurprising behaviour of this control family, unlike the wrapping main-panel `+`/`-` shortcut buttons, RQ-CTL-007); `textFromValueFunction`/`valueFromTextFunction` format the value as a plain integer. Two tokens (`patchSpinnerWidth`, `patchSpinnerTextBoxWidth`) size it — no raw literal. `Slider::keyPressed`'s native Up/Down-arrow stepping is relied on as-is, not reimplemented. **No unit test**: pure JUCE widget configuration with no headless-testable logic, same precedent as TASK-HLT-001 (no class under `juce/app/src/` has Catch2 coverage in this repo).
  - *Follow-up (2026-08-05, owner visual review): two corrections.* (1) **Off-centre and cramped.** `AlertWindow::updateLayout` always positions a custom component at `proportionOfWidth(0.1f)` and never resizes it, so the bare 92px-wide spinner sat flush-left with empty space to its right — ugly. Fixed by wrapping it in a new `CentredRow` component (`Dialogs.cpp`, anonymous namespace) sized to the new `patchSpinnerRowWidth` token (300px): since AlertWindow sizes the whole dialog to at least `customComponentWidth / 0.8`, a row wide enough to dominate that formula ends up spanning exactly the dialog's 10%..90% content column, centring the spinner inside it by construction rather than by fighting the layout afterwards. Also widened the spinner itself +15% (`patchSpinnerWidth` 80→92, `patchSpinnerTextBoxWidth` 60→69, proportionally) for readability. (2) **Free-text entry.** The stock `Label`/`TextEditor` the base `LookAndFeel` returns for a Slider's text box accepts arbitrary characters — harmless functionally (`valueFromTextFunction`'s `getIntValue()` parses garbage as 0, then `setRange` clamps it) but unprofessional. `XplorerLookAndFeel` gains a `createSliderTextBox` override (no other Slider in the app uses a text box, so this is scoped to the spinner alone) returning a new `DigitsOnlyLabel` whose `createEditorComponent()` applies `setInputRestrictions(2, "0123456789")` — non-digit keystrokes and pastes are rejected as they happen, capped at 2 characters for the model's own 0..99 range. **Status: Done** — build clean (Debug, `XPL_BUILD_APP=ON`, no warnings), full suite 125/126 both before and after these follow-ups (the 1 failure is the pre-existing environment-dependent virtual-MIDI-cable scenario, `[RQ-TST-005]`, unrelated to this change — confirmed present on a from-scratch build before any of this task's edits; 0 tests modified). Owner visually verified all three rounds (initial spinner, centring/widening, digits-only) before this commit. *Acceptance (Gherkin):* Given the "Go to patch" or "Store" dialog, When it opens, Then a spinner pre-filled with the current program number is shown with stacked +/- buttons and no combo box; Given the control, When a value is typed or stepped past 99 or below 0, Then it clamps to 99 or 0 and never wraps; Given the control has keyboard focus, When Up/Down is pressed, Then the value steps exactly as the on-screen buttons do; Given the dialog is accepted, When `onAccept` fires, Then it receives the control's current integer value, unchanged from the prior combo-box contract. | M | RQ-GUI-058, RQ-GUI-021, RQ-GUI-025; RQ-DSN-021, RQ-DSN-061; ADR-JUC-014 |
| TASK-GUI-015 | **Concise, number-first CC labels in the MIDI automation table** (added 2026-08-05, owner report, session GUI). `xpl_app_core`: new `ControlChangeShortNames.inc` (96 owner-curated entries, CC 0-31 and 64-127 only); `controlChangeShortName(int)` resolves the final short name at call time — CC 32-63 recurse into `controlChangeShortName(cc-32) + " (LSB)"`, any curated base name equal to the literal "Undefined" gets that CC's own number appended, CC 6/38/96/97 stay "Data Entry"/"Data Entry (LSB)"/"Data Entry +1"/"Data Entry -1" (owner correction — a bare "Data +1"/"Data -1" was rejected as meaningless); `controlChangeDisplayLabel(int)` formats `"<CC, zero-padded 3 digits> - <short name>"`, with the unassigned index (128) or any out-of-range value rendering as bare "None" (no prefix) — headless, unit-tested, no JUCE dependency, same discipline as the existing `controlChangeName`. `SettingsDialog.cpp`'s `CcCombo` (the automation table's "MIDI CC" column) switches from `controlChangeName(i)` to `controlChangeDisplayLabel(i)` for every dropdown item — no other behaviour change (selection id mapping, reset-to-unassigned, persistence all untouched). Per ADR-JUC-012 (DEC-JUC-102/103). **Status: Done** — new `[RQ-GUI-059]` scenario in `ControlTableTests.cpp` covers curated names, the Undefined-number rule, the LSB derivation, the "Data Entry" family and the "None"/out-of-range case (27 assertions, all green); full headless `xpl_tests_app` suite 35/35 test cases, 2049 assertions, 0 modified; `XplorerApp` (Debug, `XPL_BUILD_APP=ON`) rebuilds clean with `SettingsDialog.cpp`'s new call site. Visual confirmation of the rendered column is pending the owner's own look at the running app (no scripted UI verification performed). *Acceptance (Gherkin):* Given the MIDI automation table, When it is displayed, Then every "MIDI CC" cell shows its CC number first, zero-padded to 3 digits, followed by a short name; Given a CC whose reference name is "Undefined", When its label is resolved, Then the short name includes that CC's own number; Given a CC in the LSB mirror range (32-63), When its label is resolved, Then it equals its MSB counterpart's short name plus " (LSB)"; Given the unassigned entry or an out-of-range value, When displayed, Then it reads "None" with no leading number. | M | RQ-GUI-059; ADR-JUC-012 (DEC-JUC-102/103) |
| TASK-GUI-016 | **Automation table columns fill the dialog; "MIDI CC" is the only flexible one** (added 2026-08-05, owner report, session GUI). `design-tokens.yaml`/`DesignTokens.hpp`: new `midiCcColumnMinWidth` token (240, value-preserving from the column's prior fixed width) — tokenises the literal this change touches anyway. `SettingsDialog.cpp`: `getHeader().addColumn("Parameter", 1, LABEL_WIDTH, LABEL_WIDTH, LABEL_WIDTH)` (min==max, zero flexibility) and `addColumn("MIDI CC", 2, MIDI_CC_COLUMN_MIN_WIDTH, MIDI_CC_COLUMN_MIN_WIDTH, -1)` (floor, no ceiling), then `getHeader().setStretchToFitActive(true)` once. Confirmed against the vendored JUCE source: `TableHeaderComponent::resizeColumnsToFit` allocates any slack across columns proportional to their min/max range, so pinning "Parameter" to zero range hands "MIDI CC" 100% of it; `TableListBox::resized()` already drives this off `getVisibleContentWidth()`, which already excludes the vertical scrollbar when it is showing — no bespoke scrollbar arithmetic needed. Per ADR-JUC-012 (DEC-JUC-104). **No unit test**: pure JUCE widget/header configuration with no headless-testable logic, same precedent as TASK-GUI-014/TASK-HLT-001. **Status: Done** — `XplorerApp` (Debug, `XPL_BUILD_APP=ON`) rebuilds clean; the two prior columns' combined width now always equals the header's own width by construction (stretch-to-fit), eliminating the unlabelled trailing area the owner reported. Visual confirmation of the resize behaviour in the running app is pending the owner's own look (no scripted UI verification performed). *Acceptance (Gherkin):* Given the MIDI settings page at its default size, When the automation table is displayed, Then the two columns' combined width equals the table's own width with no unlabelled trailing area; Given the settings dialog is widened, When it resizes, Then "MIDI CC" grows by the same amount and "Parameter" is unchanged; Given the dialog is narrowed toward "MIDI CC"'s floor, When it resizes, Then the column stops shrinking at that floor; Given enough rows for the vertical scrollbar to show, When columns are sized, Then "MIDI CC" still excludes the scrollbar's width. | M | RQ-GUI-060; ADR-JUC-012 (DEC-JUC-104) |
| TASK-GUI-017 | **Settings dialog font sizing — combos/checkboxes/radios match their row labels** (added 2026-08-06, owner report, session GUI). Full audit first (see ADR-JUC-033 Context): `getComboBoxFont` (12px condensed, RQ-GUI-047) and `drawToggleButton`'s caption (12px `textCaption`) are both tuned for the main panel and applied unmodified to the settings dialog via the single global `LookAndFeel`; every settings-page row `Label` had no explicit font (JUCE's own 15px default). `XplorerLookAndFeel.cpp`: `getComboBoxFont`/`drawToggleButton` both branch on `component.findParentComponentOfClass<juce::DialogWindow>()` — non-null (settings/About/Dependencies) resolves `textTitle` (15px), null (main window) keeps the exact prior behaviour untouched. `SettingsDialog.cpp`: new `dialogControlFont()` helper (aliases `textTitle`), applied via `.setFont()` to every row-caption `Label` in `MidiSettingsPage`/`UiSettingsPage`/`RandomizerSettingsPage` (device/channel/synth-type/delay/automation-table/knob-LED/movement/style/block-colour/randomizer labels) — hint lines (`_unityHint`/`_blockHint`) and every `TextButton` are explicitly untouched. Per ADR-JUC-033 (DEC-JUC-105). **No unit test**: pure `LookAndFeel`/JUCE widget configuration with no headless-testable logic, same precedent as TASK-GUI-014/016. **Status: Done** — `XplorerApp` (Debug, `XPL_BUILD_APP=ON`) rebuilds clean; full headless `xpl_tests_app` suite unaffected (35/35, 2049 assertions — this change touches no headless code). Visual confirmation pending the owner's own look at the running app (no scripted UI verification performed). *Acceptance (Gherkin):* Given the MIDI settings page, When displayed, Then "Smart all notes off", every combo and every row label render at the same size; Given the User interface page, When displayed, Then the knob radios match their labels and the hints stay visibly smaller; Given the Randomizer page, When displayed, Then its checkboxes and combos match their labels; Given the main window, When rendered, Then every combo/toggle is pixel-identical to before (RQ-GUI-047 unaffected); Given the automation table and About/Dependencies, When displayed, Then their text is unchanged (`textSubtitle`). | M | RQ-GUI-061, RQ-GUI-047; ADR-JUC-033 (DEC-JUC-105) |
